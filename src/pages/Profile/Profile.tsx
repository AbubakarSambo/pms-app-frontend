import { Pane, Heading } from "evergreen-ui";

const Profile = () => {
  return (
    <Pane padding={16}>
      <Heading size={700} marginBottom={24}>
        Profile
      </Heading>
    </Pane>
  );
};

export default Profile;
